import { useState } from "react";
import "./App.css";

type User = {
  name: string;
  age: number;
  isAdmin?: boolean;
};

const App = () => {
  function doSomething(userObject: User) {}

  return <></>;
};

export default App;
